<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <style>
        body { font-family: sans-serif; text-align: center; padding: 50px; }
        h1 { font-size: 50px; color: #333; }
        .status { font-size: 100px; color: #e74c3c; margin: 0; }
        p { color: #666; font-size: 18px; }
        a { text-decoration: none; color: #3498db; border: 1px solid #3498db; padding: 10px 20px; border-radius: 5px; }
        a:hover { background: #3498db; color: white; }
    </style>
</head>
<body>
    <h1 class="status">{{ statusCode }}</h1>
    
    <h1>{{ title }}</h1>
    
    <p>错误详情: {{ message }}</p>
    
    <p>请求路径: {{ path }}</p>

    <br>
    <a href="/">返回首页</a>
</body>
</html>